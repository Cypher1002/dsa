bool findredundantBracket(string s){
    stack<char> st;
     for(int i =0 ;i<s.length();i++){
         char c = s[i];


         if(c == '(' || ch == "+" || ch =="-"|| ch =="*"|| ch =="/"   ){
             st.push();
         }
         else{
             if(ch == ")"){
                bool  isRedundant = true
                 while(st.top(!="(")){
                     char top = st.top();
                     if(top == '(' || top == "+" || top =="-"|| top =="*"|| top =="/")
                     {

                bool  isRedundant = false;
                     }
                     __STDC_IEC_559__.top();
                     
                 }
             }
         }
     }
}